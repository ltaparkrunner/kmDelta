#include "tcp_exch.h"

tcp_exch::tcp_exch(tcpMan *tm, QObject *parent)
{

}

int getCond() {

}

// void tcp_exch::getParamsButt(QString ip_t, QString port_t) {
//     ret_t rez = tcp_req::req_param();
// //    if(tcpC->request() > 0) qDebug("success /n");
//     if(tcpC->getParams(ip_t,  port_t, *(rez.bdata)) > 0) qDebug("success /n");
//     else qDebug("fault /n");
// }
int tcp_exch::connect(QString &ip, QString &port){

    return 0;
}
void tcp_exch::finishConn(){

}
int tcp_exch::sendMess(QByteArray ba) {
//     ret_t rez = tcp_req::req_param();
// //    if(tcpC->request() > 0) qDebug("success /n");
//     if(tcpC->getParams(ip_t,  port_t, *(rez.bdata)) > 0) qDebug("success /n");
//     else qDebug("fault /n");
    return 0;
}
